# Conditionals

- `if-else.js` — basic branching and nested conditions.
- `switch.js` — multi-branch scenarios with fall-through control.
- `ternary.js` — concise conditional expressions for inline decisions.

Aim to rewrite nested `if` blocks into guard clauses or switches to reduce depth.
