# Break and Continue

- `break.js` — exit a loop early when a condition is met.
- `continue.js` — skip the current iteration and proceed.
- `labeled.js` — control nested loops with labels (use sparingly).

Practice rewriting nested loops to guard clauses or array helpers when possible.
