# DOM Basics

- `selectors.js` — querySelector, querySelectorAll, traversal helpers.
- `manipulation.js` — createElement, append, insertBefore, remove.
- `classes-and-styles.js` — classList, inline styles, computed styles.

Build small HTML files to practice dynamic content rendering.
