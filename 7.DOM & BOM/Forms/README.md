# Forms

- `form-basics.js` — submit handlers, preventing default submission.
- `validation.js` — Constraint API, custom validation messages.
- `formdata.js` — collect fields and send via Fetch.

Experiment with both HTML5 validation attributes and custom JavaScript logic.
