// const formData = new FormData(formElement);
// formData.append("token", "123");
// Fetch example:
// fetch("/api", { method: "POST", body: formData });
// Remember that FormData is multipart/form-data; use JSON.stringify when APIs expect JSON.
