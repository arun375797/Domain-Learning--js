// form.addEventListener("submit", (event) => {
//   event.preventDefault();
//   const data = new FormData(event.currentTarget);
//   console.log(Object.fromEntries(data.entries()));
// });
// Reset forms with form.reset() to clear values.
