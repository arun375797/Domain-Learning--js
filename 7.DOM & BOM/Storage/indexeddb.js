// indexedDB.open("app-db", 1) returns a request; handle success, error, and upgradeneeded events.
// Use object stores for collections and transactions for reads/writes.
// Promise wrapper libraries (idb) simplify the verbose API.
