# Storage

- `localstorage.js` — persistent key/value storage.
- `sessionstorage.js` — per-tab storage.
- `cookies.js` — document.cookie helpers.
- `indexeddb.js` — structured storage for larger datasets.

Practice building tiny wrappers that handle JSON serialization and feature detection.
