# Events

- `event-basics.js` — addEventListener patterns and removing listeners.
- `event-delegation.js` — handle many elements with one listener.
- `custom-events.js` — dispatch `CustomEvent` for app-specific signals.

Practice inspecting `event.target` vs `currentTarget` to avoid confusion.
