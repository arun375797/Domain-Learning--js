// Delegate events to a parent:
// list.addEventListener("click", (event) => {
//   const button = event.target.closest("button");
//   if (!button) return;
//   console.log(button.dataset.id);
// });
// Works even as new buttons are added dynamically.
