# Browser Object Model

- `window-object.js` — globals, timers, focus/blur.
- `location-and-history.js` — navigation, pushState, popstate listener.
- `dialogs-and-clipboard.js` — alert/confirm/prompt, Clipboard API.

Combine with `Storage/` examples to build complete browser utilities.
