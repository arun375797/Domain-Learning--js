# ES Modules

- `export.js` — named vs default exports.
- `import.js` — importing modules, aliasing, dynamic import.
- `module-setup.md` — configuring Node or bundlers for ESM.

Remember to add `"type": "module"` to `package.json` in Node or use `.mjs` extensions.
