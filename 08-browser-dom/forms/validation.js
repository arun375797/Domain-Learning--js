/**
 * Form Validation
 * 
 * WHAT TO LEARN:
 * 
 * 1. HTML5 VALIDATION
 *    - required attribute
 *    - pattern attribute (regex)
 *    - type="email", "url", etc.
 *    - min, max for numbers
 * 
 * 2. CHECKVALIDITY
 *    - input.checkValidity()
 *    - form.checkValidity()
 *    - Returns boolean
 *    - Checks HTML5 validation
 * 
 * 3. SETCUSTOMVALIDITY
 *    - input.setCustomValidity('message')
 *    - Sets custom error message
 *    - Empty string clears
 * 
 * 4. VALIDITY PROPERTY
 *    - input.validity
 *    - ValidityState object
 *    - validity.valid, validity.patternMismatch, etc.
 * 
 * 5. INVALID EVENT
 *    - input.addEventListener('invalid', handler)
 *    - Fires on invalid
 *    - Before submit
 * 
 * 6. CUSTOM VALIDATION
 *    - Check in JavaScript
 *    - Set custom validity
 *    - Display errors
 */

// HTML5 validation: required, pattern, min, max, type attributes
// JavaScript validation: checkValidity(), setCustomValidity()
// Custom validation messages and error display
