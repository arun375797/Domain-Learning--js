/**
 * Form Basics
 * 
 * WHAT TO LEARN:
 * 
 * 1. FORM ELEMENTS
 *    - form, input, textarea, select, button
 *    - HTML form elements
 *    - Accessible via DOM
 * 
 * 2. FORM COLLECTION
 *    - form.elements
 *    - HTMLFormControlsCollection
 *    - All form controls
 * 
 * 3. ACCESSING ELEMENTS
 *    - form.elements.name
 *    - form.elements['name']
 *    - form.name (if name/id exists)
 * 
 * 4. SUBMIT EVENT
 *    - form.addEventListener('submit', handler)
 *    - Fires on submit
 *    - event.preventDefault() to prevent
 * 
 * 5. RESET
 *    - form.reset()
 *    - Resets form to initial state
 * 
 * 6. VALUE PROPERTY
 *    - input.value
 *    - Current input value
 *    - String type
 */

// Form elements: form, input, textarea, select, button
// Access form elements: form.elements, form.elements.name
// Form submission: form.addEventListener("submit", handler)
