/**
 * SessionStorage
 * 
 * WHAT TO LEARN:
 * 
 * 1. SESSIONSTORAGE
 *    - window.sessionStorage
 *    - Session-only storage
 *    - Cleared when tab closes
 *    - Same-origin policy
 * 
 * 2. METHODS
 *    - Same as localStorage
 *    - setItem, getItem, removeItem, clear
 *    - Same API
 * 
 * 3. DIFFERENCES FROM LOCALSTORAGE
 *    - Cleared on tab close
 *    - Not shared across tabs
 *    - Per-tab storage
 * 
 * 4. USE CASES
 *    - Temporary data
 *    - Form drafts
 *    - Tab-specific state
 * 
 * 5. LIMITATIONS
 *    - Same as localStorage
 *    - ~5-10MB
 *    - Strings only
 */

// sessionStorage: storage for session duration, same-origin
// Methods: same as localStorage
// Data cleared when tab/window closes
