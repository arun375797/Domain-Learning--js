/**
 * Classes and Styles
 * 
 * WHAT TO LEARN:
 * 
 * 1. CLASSLIST
 *    - element.classList.add('class')
 *    - element.classList.remove('class')
 *    - element.classList.toggle('class')
 *    - element.classList.contains('class')
 *    - element.classList.replace('old', 'new')
 * 
 * 2. CLASSNAME
 *    - element.className = 'class1 class2'
 *    - String property
 *    - Replaces all classes
 *    - Use classList instead
 * 
 * 3. STYLE PROPERTY
 *    - element.style.property = 'value'
 *    - Inline styles
 *    - element.style.color = 'red'
 *    - camelCase property names
 * 
 * 4. GETCOMPUTEDSTYLE
 *    - getComputedStyle(element)
 *    - Final computed styles
 *    - All CSS applied
 *    - Read-only
 * 
 * 5. CSS VARIABLES
 *    - element.style.setProperty('--var', 'value')
 *    - Set CSS custom properties
 *    - Useful for theming
 * 
 * 6. BEST PRACTICES
 *    - Use classList for classes
 *    - Prefer CSS classes over inline styles
 *    - Use getComputedStyle to read values
 */

// element.classList.add("is-active"); element.classList.remove("is-hidden");
// Toggle classes to control CSS states instead of directly editing style properties.
// Use getComputedStyle(element) to read final cascade values.
