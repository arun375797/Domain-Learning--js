/**
 * Element Attributes
 * 
 * WHAT TO LEARN:
 * 
 * 1. GETATTRIBUTE
 *    - element.getAttribute('name')
 *    - Gets attribute value
 *    - Returns string or null
 * 
 * 2. SETATTRIBUTE
 *    - element.setAttribute('name', 'value')
 *    - Sets attribute
 *    - Always string
 * 
 * 3. REMOVEATTRIBUTE
 *    - element.removeAttribute('name')
 *    - Removes attribute
 * 
 * 4. HASATTRIBUTE
 *    - element.hasAttribute('name')
 *    - Checks existence
 *    - Returns boolean
 * 
 * 5. DATA ATTRIBUTES
 *    - element.dataset.key
 *    - Access data-* attributes
 *    - data-role → dataset.role
 *    - Hyphens become camelCase
 * 
 * 6. PROPERTIES VS ATTRIBUTES
 *    - Attributes: HTML attributes (strings)
 *    - Properties: JavaScript properties (typed)
 *    - Some sync, some don't
 *    - id, className sync
 *    - value may not sync
 */

// Attributes: getAttribute, setAttribute, removeAttribute, hasAttribute
// Data attributes: dataset property (data-* attributes)
// Properties vs attributes: some attributes have corresponding properties
