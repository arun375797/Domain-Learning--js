// ES6 classes wrap prototype logic with cleaner syntax.
// class Person { constructor(name) { this.name = name; } greet() { return `Hi ${this.name}`; } }
// Support extends, super calls, static members, and private fields (#name).
