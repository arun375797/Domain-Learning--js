// Every object has an internal [[Prototype]] linking to another object.
// Use Object.create(proto) to build objects with a shared prototype.
// Explore Object.getPrototypeOf(obj) and Object.setPrototypeOf(obj) in the REPL.
