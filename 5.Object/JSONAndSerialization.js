// JSON.stringify(object) => string; optional replacer and space params for filtering/formatting.
// JSON.parse(string) => object; supply a reviver to transform values (e.g., to Date).
// Beware of circular references; use libraries or structuredClone when needed.
