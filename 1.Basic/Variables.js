// let: block-scoped, mutable; const: block-scoped, immutable binding.
// var: function-scoped; avoid unless you need hoisting for legacy code.
// Exercise: rewrite `var` examples using `let`/`const` and note scoping changes.
