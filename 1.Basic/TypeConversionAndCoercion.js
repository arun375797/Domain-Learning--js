// Explicit conversion: Number(value), String(value), Boolean(value).
// Implicit coercion: watch out for + with strings, == loose equality checks.
// Practice: build a truthy/falsy table and log results for edge cases (`"", 0, [], {}`).
