// Primitive types: string, number, boolean, null, undefined, symbol, bigint.
// Reference types: objects, arrays, functions; they share references when assigned.
// Log each type with `typeof` and note quirks like typeof null === "object".
