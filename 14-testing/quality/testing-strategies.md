# Testing Strategies

- Pyramid: unit > integration > end-to-end.
- Test naming: `should describe expected behaviour`.
- Keep tests deterministic; avoid relying on external APIs without mocks.
- Use TDD for complex logic; write regression tests after fixing bugs.
