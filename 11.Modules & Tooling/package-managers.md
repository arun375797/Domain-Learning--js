# Package Managers

- npm: default with Node.js; use `npm install`, `npm run`.
- Yarn: faster installs, workspaces; `yarn add`, `yarn dlx`.
- pnpm: disk-efficient symlinks; `pnpm add`, `pnpm install`.
- Record commands for initializing projects and upgrading dependencies.
