// Example: setTimeout(callback, delay)
setTimeout(() => {
  console.log("Executed after 1 second");
}, 1000);

// Browser APIs like geolocation also use callbacks.
// navigator.geolocation.getCurrentPosition(success, error);
