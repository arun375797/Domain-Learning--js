# Callbacks

- `callback-basics.js` — asynchronous APIs that expect callbacks.
- `error-first.js` — Node-style error-first conventions.
- `callback-hell.js` — refactor nested callbacks into promises.

Understanding callbacks makes it easier to reason about legacy code and libraries.
