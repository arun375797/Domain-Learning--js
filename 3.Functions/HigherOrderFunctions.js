// Higher-order functions accept or return other functions.
// Common examples: array.map(callback), array.filter(callback), array.reduce(callback, initial).
// Practice building your own: function repeat(fn, times) { for (...) fn(); }
